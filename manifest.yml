applications:
  # Approuter (SSO Gateway)
  - name: servicenow-approuter
    path: approuter
    memory: 128M
    buildpacks:
      - nodejs_buildpack
    env:
      destinations: >
        [
          {
            "name": "backend-api",
            "url": "https://servicenow-backend.cfapps.<region>.hana.ondemand.com",
            "forwardAuthToken": true
          }
        ]
    services:
      - servicenow-xsuaa

  # Python Dockerized Backend Service
  - name: servicenow-backend
    path: backend
    docker:
      image: <your-docker-registry>/servicenow-backend:latest
    memory: 512M
    env:
      SN_INSTANCE: "your-instance.service-now.com"
      VAULT_URL: "https://vault.yourdomain.com/v1/secret/data/servicenow"
      VAULT_TOKEN: "your-vault-token"
    services:
      - servicenow-xsuaa