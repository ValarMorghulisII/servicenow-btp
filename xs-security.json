{
  "xsappname": "servicenow-btp",
  "tenant-mode": "dedicated",
  "description": "Security configuration for ServiceNow Upload App",
  "scopes": [
    {
      "name": "$XSAPPNAME.User",
      "description": "User scope to access upload portal"
    }
  ],
  "role-templates": [
    {
      "name": "UserRoleTemplate",
      "description": "Role required to access the vulnerability upload tool",
      "scope-references": [
        "$XSAPPNAME.User"
      ]
    }
  ]
}